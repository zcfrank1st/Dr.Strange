akka {
  actor {
    provider = cluster
  }
  remote {
    log-remote-lifecycle-events = off
    netty.tcp {
      hostname = "127.0.0.1"
      port = 6000
    }
  }
  extensions = ["akka.cluster.client.ClusterClientReceptionist"]
}